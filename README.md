# Living-Commonwealth-Engine-LCE-
Building living worlds through simulation. A game-agnostic C++ core where entities have needs, memory, relationships, goals and behaviour — driven by a deterministic, tunable, saveable tick. The first platform is Fallout 4. The core never knows.
